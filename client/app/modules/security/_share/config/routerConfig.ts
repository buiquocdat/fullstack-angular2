let routerConfig = {
    permissions: { name: "Permissions", path: "/permissions" },
    addPermission: { name: "Add Permission", path: "/addPermission" },
    editPermission: { name: "Edit Permission", path: "/editPermission" }
};

export default routerConfig;